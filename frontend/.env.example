# Theme: lufthansa | default
DEFAULT_THEME=lufthansa

# Environment: development | production
VITE_ENVIRONMENT=development

# Backend API URL (only needed if not using Vite proxy)
# VITE_API_BASE_URL=http://localhost:8080
